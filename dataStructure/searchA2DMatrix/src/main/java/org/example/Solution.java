package org.example;

public class Solution {
    public boolean searchMatrix(int[][] matrix, int target) {

        int subarraySize = matrix[0].length;
        int subarrayCount = matrix.length;
        int cellSize = subarraySize * subarrayCount;
        System.out.println(cellSize);

        int i = 0;

        while (i != cellSize) {
            
        }
        return false;
    }
}
